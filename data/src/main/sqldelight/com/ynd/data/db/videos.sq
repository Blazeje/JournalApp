CREATE TABLE videos (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    fileUri TEXT NOT NULL,
    description TEXT,
    createdAt INTEGER NOT NULL
);

selectAll:
SELECT * FROM videos ORDER BY createdAt DESC;

insertVideo:
INSERT INTO videos(fileUri, description, createdAt)
VALUES (?, ?, ?);

deleteAll:
DELETE FROM videos;
